import palette from './app.pallete.js'

const button = {
  styleOverrides: {
    contained: {
      padding: '10px 16px',
      opacity: '1',
      backgroundColor: palette.primary[900]
    },
  },
}

export default button
