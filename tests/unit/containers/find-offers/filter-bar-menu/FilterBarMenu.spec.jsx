import { render, screen } from '@testing-library/react'
import FilterBarMenu from '~/containers/find-offer/filter-bar-menu/FilterBarMenu'

describe('FilterBarMenu test', () => {
  beforeEach(() => {
    render(<FilterBarMenu />)
  })
  it('should have title text', () => {
        
  })
})
