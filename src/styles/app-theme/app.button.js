import palette from './app.pallete.js'

const button = {
  styleOverrides: {
    root: {
      padding: '10px 16px',
      opacity: '1',
    },
    contained: {
      backgroundColor: palette.primary[900]
    },
  },
}

export default button
